# DMT143 Monitor 项目文档

## 项目介绍

DMT143 Monitor 是 Vaisala DRYCAP 露点变送器 DMT143（传感器 DRYCAP 180D）的专用监控软件。通过 RS485/RS232 串口实时读取露点温度、标准气压露点和体积含水量，以仪表盘、渐变条和趋势曲线直观呈现。

---

## 功能说明

### 1. 实时数据监控

| 参数 | 说明 | 量程 |
|------|------|------|
| 露点温度（Tdf） | 气体冷却至饱和时的温度 | -80 ~ +20 °C |
| 标准气压露点（Tdfatm） | 换算到标准大气压的露点 | -80 ~ +20 °C |
| 体积含水量（H2O） | 气体中水蒸气含量 | 0 ~ 30000 ppm |

### 2. 渐变热力条

每个仪表盘下方有量程占比条，颜色随数值变化：

- **Tdf / Tdfatm**：红 → 黄 → 青 → 蓝（暖色在左代表低温，冷色在右代表高温）
- **H2O**：浅蓝 → 深蓝（低浓度 → 高浓度）

条宽与数值在量程内的比例对应，最小值宽度为 0，最大值铺满。

### 3. 实时曲线

同时绘制三参数趋势曲线，支持缩放和拖拽查看历史。

### 4. 连接管理

- 自动扫描可用串口，手动刷新
- RS485 半双工 / RS232 自动适配
- 连接时严格验证设备信息 + FORM 命令
- 断开后约 8 秒检测，立即开始自动重连

### 5. 传感器状态监测

FORM 命令含 STAT 字段，实时显示传感器诊断状态：

| 状态码 | 含义 |
|--------|------|
| N | 正常 |
| A | 自动校准中 |
| H | 化学清除中 |
| h | 传感器加热中 |

### 6. 日志管理

- 运行日志实时输出到界面日志区
- 断开连接时自动保存到 `logs/` 目录
- Ctrl+L 打开历史日志查看器，支持多选删除

---

## 使用指南

### 连接设备

1. 选择串口（通常 COM5 或 COM3），波特率默认 19200
2. 点击"连接"，自动完成设备信息获取、FORM 设置、启动数据流
3. 状态栏显示 🟢 已连接 / 🔴 连接异常

### 查看日志

菜单 → 文件 → 查看历史日志（Ctrl+L）

---

## 项目结构

```
DMT143_Monitor/
├── main.py                         # 应用入口
├── requirements.txt                # Python 依赖
├── dmt143_config.json              # 用户配置（串口/波特率等）
├── icon.ico                        # 窗口图标
├── DMT143_Monitor_v2.6.3.spec      # PyInstaller 打包配置
├── DMT143 User's Guide in Chinese M211435ZH.pdf  # 官方中文手册
├── commands.md                     # DMT143 串口命令速查
├── core/
│   ├── serial_client.py            # 串口通信（connect / send / read / parse）
│   └── data_parser.py              # 数据帧解析
├── ui/
│   ├── main_window.py              # 主窗口布局 + 所有业务逻辑
│   ├── gauge_widget.py             # 仪表盘 GaugeWidget + 渐变条 GradientBar
│   ├── chart_widget.py             # 实时曲线（pyqtgraph）
│   └── dialogs.py                  # 关于 / 历史日志对话框
├── dist/                           # 打包后 exe
├── logs/                           # 运行日志
└── archive/                        # 历史版本文件
```

---

## DMT143 通信参数

| 参数 | 值 |
|------|-----|
| 波特率 | 19200 |
| 数据位 | 8 |
| 校验位 | N（无） |
| 停止位 | 1 |
| 流控 | 无 |
| 模式 | RS485 半双工（RTS/DTR=Low） |

### 数据格式

设备按 `Tdf Tdfa H2O` 格式输出，示例：

```
21.10  21.30  25626.0
```

### 自动命令序列

连接后自动执行：
1. `S` × 3 → 停止前次输出
2. `?` → 获取设备信息
3. `FORM TDF TDFA H2O` → 设置基础格式
4. `FORM TDF TDFA H2O "#t" STAT` → 追加状态字段
5. `R` → 启动连续输出

---

## 常见问题

### Q: 连接失败？
1. 确认串口未被其他进程占用（任务管理器检查残留进程）
2. 确认设备已通电、线缆正确
3. 尝试刷新串口列表

### Q: 传感器状态一直显示"正常"？
STAT 字段需要固件支持，部分旧固件可能不支持。

### Q: 数据曲线不刷新？
检查图表右上角 "A" 自动缩放按钮是否按下。

---

## 版本

**当前版本：v2.6.3**（2026-07-24）

### v2.6.3 更新
- 露点量程 -80 ~ +20 °C，H2O 量程 0 ~ 30000 ppm
- 渐变条：露点红→黄→青→蓝（暖左冷右），H2O 浅蓝→深蓝
- 连接严格验证：设备信息 + FORM 失败 → 显示异常
- 恢复 STAT 传感器状态监测
- 单位与数值同行显示
- 字体缩小 1-2pt，去除面板外框，设备信息 2 行横排
- 弹性仪表盘，最小窗口 1024×620
- 断线检测 8s，无冷却直接重连

### v2.6.1
- 线程安全修复、H2O 低值解析修复、图表渲染优化

### v2.5
- 界面简化、日志管理增强

---

**Power By QianYiHui**

**DMT143 是 Vaisala Oyj 的注册商标。**
