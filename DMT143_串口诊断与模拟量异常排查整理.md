# DMT143 串口诊断与模拟量异常排查整理

## 1. 设备概述

维萨拉 `DMT143` 是一款工业用露点变送器/露点传感器，主要用于测量压缩空气、干燥系统及其他低湿度气体中的露点温度。

常见特点包括：

- 适合低露点测量
- 体积紧凑，适合安装到设备或管路中
- 稳定性较好，长期漂移较小
- 常用于压缩空气系统、干燥机、塑料干燥、电子制造等场景

通常关注的信息有：

- 测量范围
- 输出方式
- 安装方式
- 校准维护要求
- 与其他同类型号的区别

## 2. 是否可以通过串口命令检测传感器问题

可以，但只能排查一部分问题，不能完全替代现场检查。

通过串口命令，通常可以完成以下诊断：

- 读取当前测量值，查看露点、温度等数据是否异常
- 读取当前设备状态或错误信息
- 查看配置参数是否正确
- 确认设备是否在线、通信是否正常
- 读取型号、固件版本、序列号等基础信息

但串口命令通常无法完整发现以下问题：

- 供电不稳
- 接线接触不良
- 采样气路泄漏
- 探头污染、老化、结露
- 安装位置不当
- 工况超出传感器适用范围

因此，更准确的说法是：

> 串口命令适合做“通信、参数、状态、读数”层面的诊断；  
> 硬件、安装、气路、污染类问题，还需要结合电气检查、工况核对和标定结果一起判断。

## 3. DMT143 串口诊断的核心思路

对 `DMT143` 做串口诊断时，建议遵循以下顺序：

1. 先确认串口通信是否正常
2. 再确认设备身份、固件和模式
3. 读取当前错误信息
4. 读取实时测量值
5. 如怀疑模拟输出异常，再检查模拟输出配置与测试结果

## 4. 串口连接基本条件

`DMT143` 常见默认串口参数为：

- 波特率：`19200`
- 数据位：`8`
- 校验位：`N`
- 停止位：`1`

即常说的：

```text
19200 8N1
```

接口通常为：

- `RS-485`

如果通信失败，优先检查：

- 波特率、校验位是否正确
- `RS-485` 正负端是否接反
- 供电是否正常
- 设备是否处于 `POLL` 模式

## 5. 关键串口命令清单

以下是前面聊天中整理出的最实用命令。

### 5.1 设备基础信息与通信确认

- `HELP`：显示支持的命令列表
- `SYSTEM`：显示设备和固件详细信息
- `VERS`：显示固件版本
- `SNUM`：显示序列号

### 5.2 错误与状态检查

- `ERRS`：显示当前激活的错误信息
- `SMODE`：查看串口工作模式
- `RESET`：重启设备
- `OPEN <地址>`：在 `POLL` 模式下打开指定地址设备
- `CLOSE`：关闭当前 `POLL` 模式连接

### 5.3 测量值检查

- `SEND`：输出一次当前测量值
- `R`：连续输出当前测量值
- `FILT`：查看或设置测量滤波
- `PRES`：查看或设置压力补偿

### 5.4 模拟输出相关命令

- `AMODE`：查看模拟输出模式
- `ASEL`：查看或设置模拟输出参数及量程
- `AERR`：查看或设置故障时模拟输出行为
- `ATEST <值>`：强制输出测试值
- `ATEST`：退出测试模式，恢复正常输出

## 6. 串口诊断命令清单 + 正常/异常返回判读表

### 6.1 基础连通性与设备身份确认

| 命令 | 作用 | 正常判读 | 异常判读 |
|---|---|---|---|
| `HELP` | 查看支持命令 | 能返回命令列表，说明串口和协议基本正常 | 无返回、乱码、超时，优先查串口参数、接线、供电 |
| `SYSTEM` | 查看设备名、软件名、版本日期等 | 能返回 `Device Name`、`SW Name`、`SW version` 等 | 无返回或内容异常，可能是通信不通或设备运行异常 |
| `VERS` | 查看固件版本 | 返回类似 `DMT143 x.x.x` | 无返回或乱码，优先检查通信参数 |
| `SNUM` | 查看序列号 | 能返回设备序列号 | 无返回，优先查通信或设备未正常响应 |

### 6.2 错误与状态判断

| 命令 | 作用 | 正常判读 | 异常判读 |
|---|---|---|---|
| `ERRS` | 读取当前激活错误 | 返回 `No errors`，说明当前没有被设备内部诊断捕获的故障 | 返回错误信息，说明设备当前存在活动故障 |
| `RESET` | 重启设备 | 重启后设备恢复响应 | 若重启后仍无响应，重点查供电、接线、硬件 |
| `SMODE` | 查看串口工作模式 | 可确认当前是否为普通模式或 `POLL` 模式 | 若设备处于 `POLL` 模式但未先 `OPEN`，很多命令可能无响应 |

### 6.3 测量值与测量链判断

| 命令 | 作用 | 正常判读 | 异常判读 |
|---|---|---|---|
| `SEND` | 输出一次当前测量值 | 测量值稳定、合理、与现场工况一致 | 数值跳变、超量程、明显不合实际，需查气路、传感器、参数 |
| `R` | 连续输出测量值 | 数值连续刷新，变化趋势符合现场工况 | 长时间固定不变、剧烈抖动、明显失真，需查测量侧问题 |
| `FILT` | 查看或设置滤波 | 滤波设置合理，响应速度与现场一致 | 滤波过大可能导致响应过慢，易误判为“传感器迟钝” |
| `PRES` | 查看或设置压力补偿 | 压力值设置与现场工况一致 | 压力补偿错误会直接导致换算结果偏差 |

### 6.4 模拟输出链路诊断

| 命令 | 作用 | 正常判读 | 异常判读 |
|---|---|---|---|
| `AMODE` | 查看模拟输出模式 | 与现场接入方式一致，例如 `4 ... 20 mA` | 若与现场接法不一致，说明配置有误 |
| `ASEL` | 查看输出参数和量程 | 输出参数、单位、上下限与控制系统一致 | 参数映射错、量程错，会导致 PLC 或显示表读值错误 |
| `AERR` | 查看故障时模拟输出行为 | 故障行为与控制系统处理逻辑一致 | 故障电流若被当作正常信号换算，会导致“超范围” |
| `ATEST 20` | 强制输出测试值 | 回路侧应读到接近对应输出值 | 若万用表或 PLC 无法对应变化，则需查回路、接线、采集卡或输出本身 |
| `ATEST` | 退出测试模式 | 输出恢复为正常测量值 | 若无法恢复，需继续查模拟输出配置或设备状态 |

## 7. 推荐串口排查顺序

### 7.1 普通模式下建议命令顺序

```text
HELP
SYSTEM
VERS
SNUM
ERRS
SEND
AMODE
ASEL
AERR
PRES
FILT
```

如果怀疑模拟输出问题，再执行：

```text
ATEST 20
ATEST
```

### 7.2 如果设备在 `POLL` 模式

先打开目标设备：

```text
OPEN 1
```

然后诊断：

```text
ERRS
SEND
SYSTEM
```

结束后关闭：

```text
CLOSE
```

## 8. 模拟量换算露点异常的核心判断

当使用模拟量转换出的露点值出现异常、超出正常范围时，首先必须区分：

### 情况 A：串口 `SEND` 正常，但模拟量换算值异常

这通常说明：

- 传感器测量本身未必有问题
- 问题更可能在模拟输出链路或外部换算逻辑

常见原因有：

- 模拟输出量程配置错误
- PLC 或采集模块换算公式错误
- 输出模式设置错误
- 故障电流被错误地当成正常信号换算
- 回路供电不足
- 负载过大
- 接线或采集卡异常

### 情况 B：串口 `SEND` 也异常

这通常说明：

- 传感器测量本身存在异常

常见原因有：

- 压力补偿错误
- 气路泄漏
- 过滤器污染
- 传感器受潮或老化
- 工况超出正常使用范围

## 9. 模拟量异常时的重点命令

当现场表现为“露点值超出正常范围”时，建议重点执行：

```text
SEND
ERRS
AMODE
ASEL
AERR
```

如条件允许，再做：

```text
ATEST 20
ATEST
```

## 10. 模拟量异常排查逻辑

### 10.1 先看真实数字量

先读取：

```text
SEND
ERRS
```

重点判断：

- `SEND` 的露点值是否在合理范围
- `ERRS` 是否报错

如果 `ERRS` 为 `No errors`，但模拟量仍异常，优先怀疑模拟输出配置或外部换算问题。

### 10.2 检查模拟输出配置

读取：

```text
AMODE
ASEL
AERR
```

#### `AMODE`

用于确认模拟输出类型是否与现场一致，例如：

- `4 ... 20 mA`
- 电压输出模式

如果现场按电流接法使用，而设备却配置成其他输出模式，就会造成异常。

#### `ASEL`

用于确认模拟输出映射参数、单位及上下限。

例如：

- 参数：`Tdf`
- 下限：`-80 °C`
- 上限：`20 °C`

则表示：

- `4 mA = -80 °C`
- `20 mA = 20 °C`

如果 PLC 按其他范围换算，例如按 `-100 ~ 20 °C` 或参数映射错误，就会导致露点显示严重偏差。

#### `AERR`

这个命令非常关键。

当设备故障时，模拟输出可能不会继续按正常测量值输出，而是故意输出一个“故障电流”。如果 PLC 没把它识别成故障，而是继续按照露点公式硬算，就会出现：

- 露点值特别大
- 露点值特别小
- 露点值明显超出正常范围

因此：

> “超范围”有时不是测量值本身超范围，而是故障电流被错误换算成了露点值。

### 10.3 检查 PLC 或采集系统的换算公式

如果 `ASEL` 显示量程为：

- `4 ... 20 mA`
- 对应 `-80 ... 20 °C`

则 PLC 公式通常应为：

```text
露点 = 下限 + (电流 - 4 mA) / 16 mA × (上限 - 下限)
```

即：

```text
露点 = -80 + (I - 4) / 16 × 100
```

示例：

- `4 mA -> -80 °C`
- `12 mA -> -30 °C`
- `20 mA -> 20 °C`

常见公式错误包括：

- 按 `0 ... 20 mA` 计算
- 上下限写反
- 单位错误
- 将 `Tdf` 当成其他参数换算

这些都会造成“露点超范围”。

### 10.4 用 `ATEST` 验证模拟输出链

例如执行：

```text
ATEST 20
```

此时去看：

- 万用表实际测到多少电流
- PLC 模拟量原始值是多少
- PLC 最终显示的露点值是多少

完成后退出测试：

```text
ATEST
```

判读方法：

- 如果 `ATEST 20` 时万用表接近 `20 mA`，但 PLC 显示不对，问题多半在 PLC 模拟输入卡或换算公式
- 如果 `ATEST 20` 时万用表都测不到正确电流，问题多半在传感器模拟输出、回路供电、接线或负载条件

### 10.5 检查供电与回路条件

模拟量异常还有一个常见原因是回路电压不足。

需要重点检查：

- 传感器供电电压是否满足要求
- 电流环总负载是否过大
- 线路过长是否导致压降
- 接线端子是否松动
- 是否存在共地或屏蔽问题

### 10.6 如果数字量也异常，再查测量侧

如果 `SEND` 本身也异常，除了 `ERRS` 外，还应检查：

```text
PRES
FILT
```

并结合现场确认：

- 压力补偿值是否正确
- 过滤器是否污染或堵塞
- 采样点是否漏气
- 传感器是否受潮
- 管路是否存在死角或冷凝

## 11. 常见现场原因汇总

根据前面整理，`DMT143` 出现“模拟量换算露点异常、超出正常范围”时，最常见的原因包括：

1. `ASEL` 设置的量程与 PLC 换算公式不一致
2. `AERR` 设置导致故障电流输出，而 PLC 仍按正常值换算
3. `AMODE` 输出模式与现场接法不一致
4. 回路供电不足或总负载过大
5. PLC 模拟输入通道类型设置错误
6. `SEND` 正常但模拟量链路异常
7. `SEND` 也异常，实际是测量侧问题而不是输出侧问题

## 12. 推荐现场快速核查项

如果现场正在排障，建议优先收集以下信息：

- 串口 `SEND` 返回值
- `ERRS` 返回值
- `AMODE` 返回值
- `ASEL` 返回值
- `AERR` 返回值
- 万用表测得的实际输出电流
- PLC 当前显示的露点值
- PLC 的模拟量量程与换算公式

只要这几项齐全，基本可以较快判断问题属于以下哪一类：

- 传感器测量异常
- 模拟输出配置错误
- PLC 换算错误
- 回路供电或接线问题

## 13. 一组推荐的实战诊断命令

可以直接在串口工具中按以下顺序输入：

```text
HELP
SYSTEM
VERS
SNUM
ERRS
SEND
AMODE
ASEL
AERR
PRES
FILT
```

若需检查模拟输出链路，再补充：

```text
ATEST 20
ATEST
```

若设备在多机轮询模式，则使用：

```text
OPEN 1
ERRS
SEND
CLOSE
```

## 14. 总结

关于 `DMT143` 的故障排查，可以归纳为三条主线：

- 用 `ERRS` 判断设备是否自报当前错误
- 用 `SEND` 或 `R` 判断测量值本身是否正常
- 用 `AMODE`、`ASEL`、`AERR`、`ATEST` 判断模拟输出链路和换算逻辑是否正确

因此，遇到“模拟量换算后的露点值异常、超出正常范围”时，不要直接认定是传感器坏了，而要优先判断：

1. 数字量测量值是否正常
2. 模拟输出配置是否正确
3. 外部换算公式是否正确
4. 回路供电与接线是否正常

只要把这几步分开排查，通常都能比较快定位问题来源。
